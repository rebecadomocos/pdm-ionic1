import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tema1-ionic1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
